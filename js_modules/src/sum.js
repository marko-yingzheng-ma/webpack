const sum = (a, b) => a + b;

/**
 *  Module System |  Common Syntax
 * ---------------|----------------------------------------
 * CommonJS(node) |   module.exports, require
 *    AMD         |   define, require
 *  ES2015 (es6)  |   export, import
 */

export default sum;